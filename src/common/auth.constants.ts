// Shared demo password assigned to all seeded users so the login screen's
// "Demo accounts — one click" buttons work. New users created through the app
// instead receive a randomly generated temporary password (see UsersService).
export const DEMO_PASSWORD = 'malta123';

export const BCRYPT_ROUNDS = 10;
